# Copy to .env for local development. Never commit .env.
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug
# Matches docker-compose.yml (local only; not a real secret)
DATABASE_URL=postgres://ongem:ongem@localhost:5432/ongem
